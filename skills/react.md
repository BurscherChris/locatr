# React

Inspect existing frontend architecture before changing code. Follow existing component patterns. Reuse existing components, hooks, and utilities. Avoid unnecessary dependencies. Respect existing state management. Respect existing i18n/translation architecture. Avoid hardcoded user-facing strings when i18n exists. Keep components focused. Preserve existing UX unless the task explicitly changes it. Add or update relevant frontend tests. Run the project's existing frontend validation commands. Do not introduce a new framework or library unnecessarily.